<% form_for(@wiki_page, :url => wiki_pages_path) do |form| %>
  <%= render :partial => form %>
  <div class="operations">
    <%= form.submit 'Create' %> | <%= link_to 'Cancel', wiki_pages_path %>
  </div>
<% end %>
